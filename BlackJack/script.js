let cards = [


]

function createCard (cardName, cardNumber, cardFace, cardValue) {
    let cardInfo {
        Name: cardName,
        Number: cardNumber,
        Face: cardFace,
        Value: cardValue
    }

    createCard.append(cardInfo)
}